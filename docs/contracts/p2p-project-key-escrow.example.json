{
  "_comment": [
    "PROPOSAL, not implemented. P2P Stage 1, first slice. The project key: who holds it,",
    "how a member gets it, how the owner rotates it, what happens when a member leaves.",
    "",
    "Started here because it is the one place a whole project can be lost. Every other",
    "failure in the design loses a snapshot or a connection; losing the key loses",
    "everything ever encrypted under it.",
    "",
    "Authority: ledger decisions of 2026-09-04 (d6758d5..3647b8a). Terms from there:",
    "  эх үүсвэр                        = the working file (DWG/RVT/MAX). Never travels.",
    "  эх үүсвэрээс гаралтай мэдээлэл   = derived output (sheet package, meta, quantities).",
    "                                     This is what travels, encrypted under the project key.",
    "",
    "Server (erk-s.mn) holds licence, membership, the live layer and statistics. It never",
    "sees output content. The escrow ring below is membership data, not content: it holds",
    "WRAPPED keys the server cannot open."
  ],

  "identities": {
    "_comment": [
      "Addressing is by id, never by IP, and there are TWO ids that must not be merged:",
      "a SEAT (botId, server-issued, outlives the machine) and a MACHINE (deviceKeyId, the",
      "hash of its own signing key, self-certifying). One key pair per device, kept in",
      "Windows Credential Manager under that Windows user - the same boundary the SSO",
      "identity record uses (one Windows user = one device).",
      "",
      "A seat moves between machines; the wrapped key below therefore names BOTH, and moving",
      "a seat to a new laptop means re-wrapping its entry to the new device key."
    ],
    "botId": "bot_7f3a91c4e85b4d2f",
    "_botId": [
      "THE SEAT. Issued by the server, and it OUTLIVES ANY ONE MACHINE: assignments and the",
      "career record hang off it, so a person changing laptops keeps their history.",
      "",
      "RESOLVED 2026-09-04 with SRV and Master, correcting an earlier instruction that the bot",
      "code should BE the public-key hash. Folding the two together would have broken a seat's",
      "history on every device swap - the seat would have been reborn as a stranger."
    ],
    "deviceKeyId": "K7QM4XR2TBD9WNJ5",
    "_deviceKeyId": [
      "THE MACHINE. base32(sha256(signing public key)), 16 characters. One per device, chosen",
      "by nobody: whoever can sign with the key IS this id, so a peer verifies it without",
      "asking a server - which is what makes a LAN work with no help at all.",
      "",
      "This is the slot today's deviceFingerprint occupies. The name and shape stay; what",
      "changes is where the value comes from - a hash of machine facts (copyable, unsignable)",
      "becomes a key pair (neither). Implemented: StudioDeviceKeyPair."
    ],
    "botPublicKey": "<ECDSA P-256 SubjectPublicKeyInfo, base64>",
    "botKeyAgreementKey": "<ECDH P-256 SubjectPublicKeyInfo, base64>",
    "_twoKeys": [
      "One signs (snapshots, invitations), one agrees (wrapping the project key). Never the",
      "same key for both.",
      "",
      "CORRECTED 2026-09-04, measured rather than assumed: this file first named Ed25519 and",
      "X25519, and .NET 9's BCL has neither. It has ECDSA and ECDH over the NIST curves,",
      "AES-GCM and HKDF. Keeping the original names would have meant adding a crypto",
      "dependency to the client for no property the design needs - it needs 'sign' and 'agree",
      "on a shared secret', and P-256 gives both. Implemented in StudioDeviceKeyPair."
    ],
    "projectCode": "prj_4d81e2a7c3f0",
    "_projectCode": [
      "The project's stable name across every device and network. Derived from the founder's",
      "signing key + a random salt at creation, so two founders cannot collide and a code",
      "cannot be guessed from a name. NOT the local Guid.NewGuid in project.erksproject",
      "(RFC-003 §35: that one is user-authored and proves nothing) and NOT the server's",
      "srv_prj id (that one needs the server). This code works offline and on a LAN."
    ]
  },

  "projectKey": {
    "_comment": [
      "One symmetric key per project per key version. Encrypts every snapshot payload.",
      "It exists in the clear ONLY inside a running Studio that has unwrapped it - never in",
      "a file, never on the server, never in a snapshot."
    ],
    "algorithm": "AES-256-GCM",
    "keyVersion": 3,
    "_keyVersion": [
      "Monotonic per project. Rotated by the owner (below). Every snapshot names the version",
      "it was sealed under, and a device keeps the history of versions it was given, so old",
      "snapshots stay readable after a rotation. Nothing is re-encrypted on rotation."
    ]
  },

  "escrowRing": {
    "_comment": [
      "WHERE THE KEY LIVES: wrapped once per member, to that member's ECDH key. The ring",
      "is the project's membership list with a wrapped key beside each entry.",
      "",
      "The ring is stored in the LIVE LAYER on the server (membership is server-owned, and",
      "joining/leaving is exactly when the ring changes) AND carried inside every snapshot",
      "manifest, so a device that is offline for a month still has the ring as of its last",
      "snapshot. Two copies, one meaning: the server's is canon, the snapshot's is the",
      "offline fallback."
    ],
    "projectCode": "prj_4d81e2a7c3f0",
    "keyVersion": 3,
    "ringSequence": 41,
    "_ringSequence": "Server-issued counter (the live layer's CAS). An edit based on a stale sequence is refused; the owner pulls and redoes it. Offline, the ring is read-only.",
    "owner": "bot_7f3a91c4e85b4d2f",
    "members": [
      {
        "botId": "bot_7f3a91c4e85b4d2f",
        "deviceKeyId": "K7QM4XR2TBD9WNJ5",
        "role": "Owner",
        "wrappedKey": "<AES key sealed to this bot's ECDH P-256 key: ECDH -> HKDF -> AES-GCM, base64>",
        "wrappedAtUtc": "2026-09-04T09:00:00Z",
        "wrappedBy": "bot_7f3a91c4e85b4d2f"
      },
      {
        "botId": "bot_c21e0a9b7d4f6e31",
        "deviceKeyId": "P3XV8ZC1HFB6MRT4",
        "role": "Member",
        "wrappedKey": "<sealed to bot_c21e...'s ECDH P-256 key>",
        "wrappedAtUtc": "2026-09-04T09:05:00Z",
        "wrappedBy": "bot_7f3a91c4e85b4d2f"
      }
    ],
    "ringSignature": "<ECDSA P-256 over (projectCode, keyVersion, ringSequence, members[]) by the owner>",
    "_ringSignature": [
      "The owner signs the ring. A member accepts a ring only if it is signed by the owner",
      "key it already trusts for this project (learned from the invitation). So the server",
      "cannot add a member, and a member cannot add a member - only the owner's key can,",
      "wherever the ring happens to be stored. [[self-declared-fields-prove-nothing]]"
    ]
  },

  "invitation": {
    "_comment": [
      "How a member gets in. The owner signs an invitation naming the invitee's bot code;",
      "the invitee presents it; the owner (or the server's live layer, on the owner's behalf",
      "using the owner's pre-signed ring update) adds the wrapped key. The invitee's device",
      "key never leaves the invitee's machine - only its PUBLIC half travels."
    ],
    "projectCode": "prj_4d81e2a7c3f0",
    "inviteeBotId": "bot_c21e0a9b7d4f6e31",
    "inviteeDeviceKeyId": "P3XV8ZC1HFB6MRT4",
    "inviteeKeyAgreementKey": "<ECDH P-256 SubjectPublicKeyInfo, base64>",
    "role": "Member",
    "issuedBy": "bot_7f3a91c4e85b4d2f",
    "issuedAtUtc": "2026-09-04T09:04:00Z",
    "expiresAtUtc": "2026-09-11T09:04:00Z",
    "signature": "<ECDSA P-256 by the owner over all fields above>",
    "_deliveredAs": "A QR code or a short code typed in. Contains the fields, not the project key - the key arrives only as a wrapped entry in the ring, sealed to the invitee's own key.",
    "_acceptance": "Two-sided, same rule as RFC-003 §3: the owner invites, the person accepts on their own device. Nobody accepts on someone's behalf."
  },

  "leaving": {
    "_comment": [
      "A member leaves (or is removed). Their ring entry is deleted AND the owner rotates:",
      "new keyVersion, new AES key, re-wrapped for every remaining member, new ring sequence.",
      "",
      "Why rotate and not just delete the entry: the departed device still holds the old",
      "key in memory/history and can read any snapshot sealed under it. Deleting the entry",
      "stops them getting FUTURE keys; rotation stops them reading FUTURE snapshots. Both",
      "are needed; neither is enough alone."
    ],
    "revokedBotCode": "bot_c21e0a9b7d4f6e31",
    "revokedAtUtc": "2026-09-20T14:00:00Z",
    "newKeyVersion": 4,
    "_whatTheyKeep": [
      "Everything they already received, readable, forever. Stated plainly because 'remove",
      "from project' will be read as 'take back what they have', and no key design can do",
      "that. Revocation protects what comes next, not what already went out."
    ],
    "_offlineRevocation": [
      "Revocation reaches a device through the ring (server pull, or the ring inside the",
      "next snapshot it receives). A revoked device that never reconnects never learns -",
      "and never receives anything new either, because nobody has its wrapped key for v4.",
      "So the limit is symmetric: it cannot read new, it cannot be told. Acceptable."
    ]
  },

  "ownerKeyLoss": {
    "_comment": [
      "The founder's device is lost/wiped. This is ledger case (в) - the one case the server",
      "is 'help' for. Without something, the project has no owner key: nobody can rotate,",
      "invite, or sign a ring. The wrapped keys remain, so MEMBERS keep working; what is",
      "lost is administration.",
      "",
      "Mechanism, same pattern as the package passphrase (PROJECT-PACKAGE-SUCCESSION §8):",
      "at project creation the owner's signing key is sealed to a one-time recovery code",
      "shown once. The sealed blob is kept in the live layer (server holds it, cannot open",
      "it). Recovery = new device + code. No code, no recovery - the product says so at",
      "creation, once, in one sentence."
    ],
    "recoverySealedOwnerKey": "<owner ECDSA P-256 private key sealed to a key derived from the recovery code, base64>",
    "recoveryCodeShownOnce": "7K4M-92XR-1TBD-Q5WN",
    "_noStrengthMeter": "Generated, not chosen. No complexity rules - the PIN rule, applied again."
  },

  "_whatThisDoesNotCover": [
    "The live layer itself (project name, members, album composition, org info) - that is",
    "server-canon, writer pushes, reader pulls, online-only. This file only says where the",
    "escrow RING sits inside it.",
    "",
    "Transport of snapshots - see p2p-transport-lan-and-carrier.example.json.",
    "The snapshot container - see p2p-snapshot-container.example.json.",
    "",
    "Hole punching / NAT traversal - out of Stage 1 by decision.",
    "",
    "RESOLVED: botId and deviceKeyId are NOT one value, and merging them was the mistake -",
    "see _botId / _deviceKeyId above."
  ]
}
